# Development (SQLite, no setup needed)
DATABASE_URL=sqlite:///./dev.db

# Production (PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/audiovisual_db

# CORS: origins permitidos (separados por coma)
# Dev: dejar el default (localhost:5173)
# Prod: poner la IP o dominio del droplet
# ALLOWED_ORIGINS=http://TU_IP_O_DOMINIO,https://TU_IP_O_DOMINIO
